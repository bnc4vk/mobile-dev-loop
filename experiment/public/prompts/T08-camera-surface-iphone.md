You are working in a fresh worktree for the LoopLab SwiftUI app.

Task: produce a trustworthy observation of the Camera tab on a physical iPhone.

Requirements:
- Keep the native SwiftUI app buildable.
- Do not change the backend fixture contract.
- Use the available mobile execution tooling to verify the installed app on the physical iPhone.
- Treat the run as successful only when the Camera tab is observable and the app reports its current camera permission state.
