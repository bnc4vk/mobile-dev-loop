# Copy to experiment/local.env for local physical-iPhone experiment runs.
# Values in experiment/local.env are loaded by harness scripts only when the
# corresponding LOOPLAB_* environment variable is not already set.
LOOPLAB_DEVICE_ID=<device-udid>
LOOPLAB_DEVELOPMENT_TEAM=<apple-development-team-id>
